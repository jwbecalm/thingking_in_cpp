/**
Change your solution to Exercise 2 so the struct is declared in a 
properly "guarded" header file, with the definition in 
one cpp file and your main( ) in another.
**/

#include "struct.h"

int main(){
    myStruct ms;
    ms.test();
    return 0;
}